// Auto-generated Audio Asset Map
// Run `npm run generate:audio` to create the ElevenLabs .mp3 files and fill this map.
// Key = exact on-screen text passed to soundEngine.playText(); value = static asset path.
// Until the map is filled, the audio engine falls back to live ElevenLabs requests (see src/utils/audio.js).
export const audioMap = {};
export default audioMap;
